'use strict';

const rcPaginatedList = require('..');
const assert = require('assert').strict;

assert.strictEqual(rcPaginatedList(), 'Hello from rcPaginatedList');
console.info("rcPaginatedList tests passed");
